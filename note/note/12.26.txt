success
$('#table').bootstrapTable("destroy");
$('#table').bootstrapTable({
	data: res.list
});


액션 인크루드 태그는 2개의 jsp가 만들어지기 때문에 변수를 서로 공유할 수 없다.
디렉티브 인크루드는 1개의 jsp가 만들어지기 때문에 변수를 공유할 수 있다.
하지만 request나 parameter등등은 액션 인크루드태그의 2개의 jsp가 같은 request를 공유한다.


CREATE TABLE `test_info`(
ti_num INT(11) UNSIGNED  not NULL AUTO_INCREMENT,
`ti_name` VARCHAR(100) NOT NULL,
`ti_addr` VARCHAR(200) NOT NULL,
`ti_etc` VARCHAR(1000) NULL DEFAULT NULL,
PRIMARY KEY(`ti_num`)
)
COLLATE='utf8_general_ci'
ENGINE=InnoDB
AUTO_INCREMENT=1
;

ALTER TABLE test_info
ADD COLUMN(ti_updcnt INT(11) UNSIGNED DEFAULT 0);


@ConfigurationProperties(prefix="spring.datasource.hikari") // properties의 "spring.datasource.hikari로 시작되는 것을 싹다 가져옴

@Bean // 요거를 쓰면 내가 직접만든 매서드(@Resource,@Service,@Repository등등..)가 아니더라도 자동으로 메모리 생성해줌.
public DataSourceTransactionManager txManager() { // 얘한테 트랜젝션 시키는것
	return new DataSourceTransactionManager(getDS());
}

@Bean


2019-12-26 16:14:43.811  INFO 4768 --- [p-nio-80-exec-4] com.bdi.sb.aop.LogAOP                    : join point=>execution(String com.bdi.sb.controller.ViewController.home())
// join point는 매서드 실행되기 전후에 aop넣기위해 사용되는 것.